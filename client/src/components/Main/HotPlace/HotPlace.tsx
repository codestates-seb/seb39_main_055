import styled from "styled-components";

const Container = styled.div`
  border: 1px solid black;
  height: 500px;
`;

const HotPlace = () => {
  return <Container>요즘 많이 찾는 핫플</Container>;
};

export default HotPlace;
