import styled from "styled-components";

const Container = styled.div`
  border: 1px solid black;
  height: 500px;
`;

const Pick = () => {
  return <Container>윗펫 PICK</Container>;
};

export default Pick;
